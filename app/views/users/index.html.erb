<h1>Users</h1>

<div class="ui three cards">
  <% @users.each do |user| %>
    <div class="card">
      <div class="content">
        <div class="header"><%= user.name %></div>
        <div class="description"><%= user.email %></div>
      </div>
      <%= link_to 'Add Friend', friendships_path(friend_id: user), method: 'post', class: 'ui green attached button' %>
    </div>
  <% end %>
</div>